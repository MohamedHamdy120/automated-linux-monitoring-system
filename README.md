# automated-linux-monitoring-system
A Bash-based Linux monitoring system that collects system metrics such as CPU, memory, disk usage, and uptime, and logs them with timestamps in a structured format. It also includes basic alerting when system thresholds are exceeded.
